import React from "react";
const prefix = "home";
const HomeComponent = () => {
  return (
    <div className={prefix}>
      <h3 className={`${prefix}__title`}>Hello </h3>
    </div>
  );
};

export default HomeComponent;
